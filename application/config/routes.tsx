import { BlogIcon, PhotoIcon, NoteIcon } from "@kapok/icons";

const routes = [
  { path: "/blog", name: "Blog", component: BlogIcon },
  { path: "/photos", name: "Photos", component: PhotoIcon },
  { path: "/notes", name: "Notes", component: NoteIcon },
];

export { routes };
